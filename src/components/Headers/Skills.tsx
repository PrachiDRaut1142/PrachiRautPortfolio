// Skills.tsx
import React, { forwardRef } from 'react';

const Skills = forwardRef<HTMLDivElement>((props, ref) => {
  return (
    <div ref={ref}>
      <h1>Skills</h1>
      {/* Your Skills component JSX */}
    </div>
  );
});

export default Skills;
